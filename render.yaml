services:
  - type: web
    name: plateforme de don
    env: php
    plan: free
    buildCommand: |
      composer install
      php artisan config:cache
      php artisan migrate --force
    startCommand: php artisan serve --host=0.0.0.0 --port=$PORT